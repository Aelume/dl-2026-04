# 原始数据目录

运行 `scripts/main.py` 前，请将原始 GC10-DET 数据放在本目录下。

期望的本地目录结构如下：

```text
data/raw/
├── GC10-DET.zip
└── GC10-DET/
    ├── 1/
    ├── ...
    ├── 10/
    └── lable/
```

原始压缩包和解压后的图片文件体积较大，因此不纳入 Git 跟踪。复现时只需要按上述结构放置原始数据，再执行主脚本即可。
